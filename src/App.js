import React from 'react';
import Signup from './signup';

function App() {
  //const mql = window.matchMedia('(max-width: 600px)');
  //let mobileView = mql.matches;
  //if (mobileView) {
  return (
    <Signup />
  )//;} else {
  //  return (
  //    <div><center><h1 style={{color: 'tomato', paddingTop: '10%'}}>Sorry, Use it in mobile until desktop development ready.</h1><br></br></center></div>
  //  );
  //}
}

export default App;
